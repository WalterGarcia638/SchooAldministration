export interface StudentGradeDetail {
    id: number;       // Identificador único del detalle
    studentId: number; // ID del estudiante
    gradeId: number;   // ID del grado
    section: string;   // Sección (grupo) del estudiante en el grado
  }
  